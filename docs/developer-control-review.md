# Developer-control review

<!-- Generated by scripts/developer-control-review.py from docs/developer-control/review.json. Edit the JSON, then run the script. CI checks that every WIT interface is reviewed and that this file matches. -->

Developer ownership and control is the default (IC-175, IC-176). Krate enforces a boundary only where portability, the recipient's permission, OS safety, evidence, the platform, or the law requires it; everything else is the application's choice, and for every enforced boundary a lower-level portable interface is named or its absence is recorded as a gap.

## Justification classes

- **evidence**: a claim about the app must rest on something a check can read
- **legal**: a law or a licence requires it
- **os-safety**: the host, the person's other data and other apps must not be reachable by an app that was not granted them
- **platform**: the operating system offers this one way, and Krate cannot offer another
- **portability**: the same file must run on every host Krate supports, so the shape every host lowers to is fixed
- **recipient-permission**: the person running the app decides what it may reach; the boundary is where they decide

## Summary

- 50 boundaries reviewed, 41 of them WIT interfaces (41 declared under wit/krate).
- 31 compulsory, each with a justification or a recorded gap; 19 optional.
- 7 recorded gaps where the escape hatch does not yet exist or the boundary is narrower than a developer would want.

## Every boundary

| Boundary | Kind | Krate standardizes | The application chooses | The lower-level way | Compulsory | Why |
|---|---|---|---|---|---|---|
| `krate:audio@0.1.0/capture` | wit | the microphone boundary: opening an input is a grant the person gives | what is done with the audio: recognition, recording, analysis, by any library | raw PCM frames | yes | recipient-permission |
| `krate:audio@0.1.0/playback` | wit | the device boundary: opening an output is a grant the person gives | synthesis, mixing, codecs and formats; the app decodes with any library it likes and hands over PCM | raw PCM buffers -- no codec or mixer of Krate's is between the app and the device | yes | recipient-permission |
| `krate:audio@0.1.0/types` | wit | the sample formats and error shapes every host lowers to | what the app does with the samples | none needed: a type carries no authority | yes | portability |
| `krate:camera@0.1.0/capture` | wit | the camera boundary: opening a camera is a grant the person gives | frame processing, encoding, vision, by any library | raw frames, pulled at the app's own rate | yes | recipient-permission |
| `krate:camera@0.1.0/types` | wit | frame and error shapes every host lowers to | what the app does with a frame | none needed: a type carries no authority | yes | portability |
| `krate:fs@0.1.0/files` | wit | the filesystem boundary: paths are sandbox-relative, reachable only under a grant the person gave, and never resolve outside it | file formats, parsers, layouts on disk, any library over the bytes | the file picker (ui/dialog) for a path outside the grants, chosen by the person; store.* for the app's own data with no grant at all | yes | recipient-permission |
| `krate:fs@0.1.0/types` | wit | path, mode and error shapes every host lowers to | what the app does with a file | none needed: a type carries no authority | yes | portability |
| `krate:gfx@0.1.0/canvas2d` | wit | the handoff of drawn pixels and draw operations to the host painter, which is the only way onto the screen | any rasterizer, layout engine, text shaper or scene graph the app brings; Krate's draw calls are a convenience, not a requirement | set-pixels on a canvas or an image widget: the app renders with anything and hands over the pixels | yes | os-safety |
| `krate:gfx@0.1.0/scene3d` | wit | nothing: an optional software 3D helper | any 3D approach; the app may ignore this entirely | canvas pixels from the app's own renderer | no | -- |
| `krate:gfx@0.1.0/types` | wit | colour, rectangle and error shapes every host lowers to | everything drawn with them | none needed: a type carries no authority | yes | portability |
| `krate:io@0.1.0/args` | wit | how launch arguments reach the app | parsing and meaning of the arguments | raw strings | yes | platform |
| `krate:io@0.1.0/log` | wit | nothing: an optional structured log the host can show | any logging scheme, or stderr | stdio | no | -- |
| `krate:io@0.1.0/stdio` | wit | that a CLI app's standard streams are the host process's | what is written and read, in any format | raw bytes on streams | yes | platform |
| `krate:io@0.1.0/streams` | wit | the byte-stream resource stdio and future IO surfaces share | framing, encoding, protocols over the bytes | raw bytes | yes | portability |
| `krate:io@0.1.0/types` | wit | IO record and error shapes every host lowers to | what the app does with them | none needed: a type carries no authority | yes | portability |
| `krate:locale@0.1.0/format` | wit | nothing: optional host-backed formatting | any formatting library shipped in the app | format in the app | no | -- |
| `krate:locale@0.1.0/info` | wit | how the person's locale and timezone are read from the host | whether to honour them | ignore the host and let the person choose in the app | no | -- |
| `krate:locale@0.1.0/types` | wit | locale and formatting type shapes | what is done with them | none needed: a type carries no authority | yes | portability |
| `krate:net@0.1.0/http-client` | wit | the network boundary: a request reaches only a host the person granted, and the host and port are checked before anything dials | which servers, which APIs, which protocols over HTTP, how redirects and retries are handled (the client follows nothing on its own) | the request and response are raw bytes and headers; any protocol over HTTP is the app's | yes | recipient-permission |
| `krate:net@0.1.0/types` | wit | request, response and error shapes every host lowers to | what is sent and how it is interpreted | none needed: a type carries no authority | yes | portability |
| `krate:net@0.1.0/ws` | wit | the same boundary for a live connection: a net.connect grant to that host and port, checked at open | the protocol over the socket, the framing, the server | raw message frames | yes | recipient-permission |
| `krate:phase1@0.0.1/host` | wit | the first-generation host world, kept for the bundles that use it | none new: this world is superseded and not for new apps | the Phase 2 and Phase 3 worlds | no | -- |
| `krate:random@0.1.0/bytes` | wit | where operating-system randomness comes from | any PRNG or crate over it (rand, uuid, getrandom route here) | seed the app's own generator from it, or use none | no | -- |
| `krate:resources@0.1.0/assets` | wit | that the app's own bundled files are readable with no grant, because they are the app's | what is bundled and in what format | embed bytes in the component itself | no | -- |
| `krate:speech@0.1.0/transcription` | wit | nothing: an optional local recogniser over audio the app already holds | any model or recogniser shipped in assets, or a service the app calls itself over the network boundary | audio/capture frames processed by the app | no | -- |
| `krate:store@0.1.0/kv` | wit | that an app's own data lives in a namespace derived from its verified lineage, so a file claiming the same id cannot read it | whether to use it at all; any data model over it | fs/files under a grant with the app's own storage library, or the app's own format inside the bundle | yes | os-safety |
| `krate:store@0.1.0/secret` | wit | that a secret is encrypted at rest under the app's own key and never lands beside the person's documents | what is stored and for how long | the app's own encryption over kv or a granted file | no | -- |
| `krate:store@0.1.0/shared` | wit | the invite-code boundary: a bucket is shared only with the machines that hold its code | whether to sync at all, what is shared, or the app's own backend over the network boundary | http-client or ws to a service the developer runs | no | -- |
| `krate:store@0.1.0/sql` | wit | the same namespace rule for a database | schema, queries, whether to use SQL at all; any embedded database over fs/files under a grant | fs/files with the app's own database library | yes | os-safety |
| `krate:time@0.1.0/clock` | wit | where the host clock is read | everything computed from it | none needed | no | -- |
| `krate:time@0.1.0/sleep` | wit | a blocking sleep for CLI apps | scheduling, timers, event loops built on it or not | events/wait with a timeout in GUI apps | no | -- |
| `krate:ui@0.1.0/clipboard` | wit | that the clipboard is the person's and reached through a grant | what is put on it | none: the clipboard is the host's | yes | recipient-permission |
| `krate:ui@0.1.0/dialog` | wit | that a path outside the grants is chosen by the person in a system dialog, never named by the app | when to ask, what to do with the answer | none: this is how the person's authority reaches the app | yes | recipient-permission |
| `krate:ui@0.1.0/events` | wit | that input arrives from the host, one event at a time, in the person's own order | what every event means | none: input is the host's to deliver | yes | platform |
| `krate:ui@0.1.0/image` | wit | the pixel handoff: an image widget shows what the app hands it | any renderer, any format decoded by the app | this is the escape hatch for drawing | no | -- |
| `krate:ui@0.1.0/launcher` | wit | that opening a link goes through the person's browser and is a grant, not a process capability | which links | show the URL and let the person copy it | yes | recipient-permission |
| `krate:ui@0.1.0/menu` | wit | nothing yet: menu calls are declared and answer unsupported | any command scheme drawn in the app | the app's own drawn menus and key handling | no | -- |
| `krate:ui@0.1.0/notify` | wit | that a notification is attributed to the app the person consented to, never to a title the app chose, and is a grant | when and what | draw the message in the app's own window | yes | recipient-permission |
| `krate:ui@0.1.0/tree` | wit | nothing: an optional portable widget tree the hosts lower to native controls or paint | any UI approach; a canvas of the app's own pixels is equal to a tree | image and canvas widgets with the app's own rendering | no | -- |
| `krate:ui@0.1.0/types` | wit | widget kinds, event shapes and errors every host lowers to | what the app builds from them | none needed: a type carries no authority | yes | portability |
| `krate:ui@0.1.0/window` | wit | that windows come from the host and are closed by the person | how many, what size, what is drawn in them | none: a window is the host's | yes | platform |
| `manifest.toml and the capability declaration` | format | what an app declares it needs, so the person can be told before it runs; an undeclared call is denied | everything not declared as a capability: architecture, libraries, data model | declare narrowly; optional capabilities are asked for at feature time (CP2) | yes | recipient-permission |
| `the .krate archive` | format | one container every host opens: fixed core entries, namespaces, ceilings, a versioned profile | what the app is made of; unknown entries survive so a tool may carry its own | unknown entries are ignored by the runtime and kept by the packer | yes | portability |
| `the WebAssembly component contract` | format | that the executable is a component of a declared world importing only krate:* interfaces | the language and toolchain that produced it: anything that emits a valid component is accepted at pack, publish and open | the validator (krate_bundle::judge_bytes) is the whole contract; cargo-component is a default, not a requirement | yes | portability |
| `publisher signing and lineage` | identity | that a signature proves who signed these exact bytes and nothing more, that storage follows verified lineage, and that a fork is visibly a fork | whether to sign at all: an unsigned app runs, labelled | run unsigned; sign with the developer's own offline key, no account | no | -- |
| `the hub (publish, channels, takedowns)` | service | admission of what it will host: the same validator the runtime uses, and a takedown record with an appeal | where to host and distribute: a .krate runs from any URL or file, and the hub is one option | any web host, any file share, a link by hand; the client holds fetched bytes to their address wherever they came from | no | -- |
| `Studio and AI authoring` | workflow | nothing about the app: the authoring pack teaches the contract, and check-app measures the result | the developer edits the source by hand at any point; an AI suggestion, repair or regeneration is a proposal the developer may reject | the bundle carries its source, Cargo.lock and SDK; open it and build without Studio | no | -- |
| `krate port` | workflow | nothing: a read-only porting assistant with honest verdicts | whether to port, what to replace, which dependencies to keep | write the app directly against the SDK | no | -- |
| `resource limits and the permission wall` | runtime | that an app cannot exceed what the person granted or exhaust the machine: memory, entry counts, archive ceilings, denied undeclared calls | everything within the grants | declare what is needed; the wall explains every denial in plain words | yes | os-safety |
| `telemetry` | runtime | that nothing leaves the machine without the person's consent, and what does is anonymous and says why an open failed | an app's own analytics, over the network boundary, declared | none needed: it is opt-in | yes | recipient-permission |

## Gaps

Where a developer's choice is narrower than the principle says it should be. Each is a row above with a gap recorded; none is a rule Krate defends.

- `krate:gfx@0.1.0/canvas2d`: no portable GPU primitive exists, so a developer's own GPU renderer cannot run (IC-176: custom renderer over portable GPU primitives); scene3d is software
- `krate:net@0.1.0/http-client`: no lower-level socket primitive exists, so a developer-chosen HTTP, gRPC or GraphQL client library cannot run on Krate today (IC-175: developer-selected client over a low-level network primitive)
- `krate:net@0.1.0/ws`: as for http-client: no raw TCP or UDP primitive, so a developer's own transport cannot run
- `krate:speech@0.1.0/transcription`: the recogniser reads one model family; another engine means the app ships its own
- `krate:ui@0.1.0/clipboard`: wired on Linux only today; other hosts answer unsupported
- `krate:ui@0.1.0/menu`: set-items answers unsupported on every host (IC-745)
- `the WebAssembly component contract`: the SDK and Studio build with cargo-component only; a developer's own build tool is accepted by the validator but not by the make flow (IC-175: custom build tool)
