[advisories]
# Path to the advisory database; fetched on first run.
version = 2
ignore = [
    # paste is archived, not vulnerable. It reaches us only through nokhwa,
    # the Windows/Linux camera backend, as a proc-macro used at compile time.
    # Drop this when nokhwa moves off it.
    "RUSTSEC-2024-0436",
]

[licenses]
version = 2
# Licenses we explicitly allow.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MPL-2.0",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "CC0-1.0",
    "Zlib",
    "OpenSSL",
    # webpki-roots ships Mozilla's CA certificate store, which Mozilla
    # publishes under CDLA-Permissive-2.0. It is certificate data rather than
    # code, and it arrives with the TLS stack that lets `krate run <https url>`
    # verify who it is downloading a bundle from.
    "CDLA-Permissive-2.0",
    # clipboard-win (arboard's Windows clipboard backend, pulled in to give the
    # drawn path Cut/Copy/Paste) is published under the Boost Software License,
    # a permissive OSI-approved license with no copyleft or attribution burden.
    "BSL-1.0",
    # whisper-rs and its whisper.cpp bindings use the Unlicense. It is an
    # OSI-approved public-domain dedication and permissive fallback license.
    "Unlicense",
]
# Confidence threshold for license expression detection.
confidence-threshold = 0.8
# Phase 0 has no dependencies yet, so most future-facing allow-list entries are
# naturally unused. Keep CI quiet until real crates arrive in Phase 1.
unused-allowed-license = "allow"

[bans]
# Warn when the same crate version appears multiple times.
multiple-versions = "warn"
# Deny wildcard dependencies (e.g. `dep = "*"`).
wildcards = "deny"
# ...but not the workspace's own crates depending on each other by path.
#
# A path dependency with no version reads as `*` to this check, and every one
# of the 53 in this workspace is that shape. They are not a supply-chain risk:
# the path names a directory in this repository, so what gets built is whatever
# is checked out beside it -- there is no registry resolution for a wildcard to
# widen, and every one of these crates is publish = false. Writing a version
# beside each path would satisfy the check by adding a number nothing reads,
# and one that goes stale the next time the workspace version moves.
#
# What this does NOT relax is a real `dep = "*"` from crates.io, which is the
# risk the rule is for and is still denied.
allow-wildcard-paths = true
# List of crates that are never allowed.
deny = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
