# Advisory scan

Scanned: 2026-09-07T10:52:51Z
Commit:  fe64ccda8b26b098e9b1934149f0a479d6b920e8
Scanner: cargo-deny 0.19.4
Verdict: ok

Generated by scripts/record-advisory-evidence.sh. Do not hand-edit:
a record that disagrees with the scanner is worse than none.

## What the scanner said

```
advisories ok
```

## Accepted matches, and why each one is not reachable

An ignored advisory is a decision. Each one below names the path that
brings it in, so the reasoning can be checked rather than trusted.

### RUSTSEC-2024-0436

Reason recorded in deny.toml:

    paste is archived, not vulnerable. It reaches us only through nokhwa,
    the Windows/Linux camera backend, as a proc-macro used at compile time.
    Drop this when nokhwa moves off it.

Dependency path (`cargo tree -i <crate> --target all`):

```
    paste v1.0.15 (proc-macro)
    └── nokhwa v0.10.11
        └── krate-runtime v0.4.0 (/crates/runtime)
            ├── krate-adapter-web v0.4.0 (/crates/adapter-web)
            ├── krate-cli v0.4.0 (/crates/cli)
            ├── krate-player-android v0.4.0 (/crates/player-android)
            ├── krate-player-ios v0.4.0 (/crates/player-ios)
            └── krate-tools v0.4.0 (/crates/tools)
```

## What this does not tell you

A clean scan means no advisory matched a crate in the graph today. It
is not a statement that the code is free of defects, and it goes stale
the moment the advisory database moves -- which is why the scan is
dated and re-run rather than filed once.
